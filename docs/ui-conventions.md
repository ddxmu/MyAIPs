# UI conventions

Read this before changing QActions, dialogs, the options bar, list-row widgets, status messages, application QSS, or other shared UI behavior.

Visible automation permits Preferences, About and AI Setup through the dialog
helpers. The activity guard permits inspection but explains conflicting actions.
An editable Pause restores ordinary manual dialog and save-prompt behavior. See
[automation-feedback.md](automation-feedback.md) for API lifetime and resume rules.

## Color scheme

MyAIPs ships Dark and Light, chosen in Preferences (`preferences/colorScheme`, values `system`/`dark`/`light`, default `dark`). Switching applies live; nothing requires a restart. `ThemeManager` in `src/ui/theme_manager.hpp` owns the preference, resolves "follow system" through `QStyleHints::colorScheme()`, and emits `color_scheme_changed`. It also mirrors the resolved scheme onto Qt with `QStyleHints::setColorScheme`; the native-chrome and offscreen consequences live in [platform.md](platform.md).

Standing rules:

- **No hex literals for chrome anywhere in `src/ui`.** Colors are named roles in `src/ui/theme_palette.hpp`. QSS writes them as `@role_name` tokens; painted widgets and delegates read `theme().role_name`. Adding a role means the member, a Dark value, and a row in the role table; `ui_theme_palettes_define_every_role` fails if you miss one.
- **Dark is authored, Light is derived then corrected.** `light_palette()` mirrors the lightness of every Dark role, lifts the mirrored neutrals into Light's surface band, and then overrides the ones where that is wrong (the canvas backdrop, bevels, brand and state colors, icon ink). A new role therefore needs no Light decision unless it deserves one.
- **The lift is what makes Light look lit.** Dark packs its surfaces into the bottom third of the value range, so a plain mirror parks every panel and toolbar in the middle grays and the app reads as medium gray rather than light. `lift_to_surface_band()` raises that end only; ink mirrors below the first stop and passes through untouched. It is monotonic, so roles cannot swap order, but it compresses near white.
- **A mirror inverts an outline.** A line darker than its surface in Dark comes back lighter than it in Light, which on a near-white surface is no line at all. Those roles are corrected by hand in `light_palette()` (field wells, the tool column and dock outlines, the splitter groove); a line that is *lighter* than its surface in Dark is ink and mirrors correctly. When adding a role that draws a line, check which of the two it is.
- **A mirror also inverts lightness that carries meaning.** Where a role says "this one is selected" or "this is the control" by sitting lighter than its neighbours, the flip keeps the value and drops the claim. Those roles are restated by hand in `light_palette()`: the selected tab rises above the strip and the unselected tabs drop below it, and the scroll-bar handle becomes the dark element in a light groove. A role whose lightness is only a surface level mirrors correctly; one whose lightness is a statement about state or elevation has to be re-decided for Light.
- **A tint on a surface is not a brand color.** `flip_for_light()` chooses between lifting a role into Light's surface band and pulling it to a mid tone by saturation alone, so a near-black wearing a cool cast trips the chromatic branch and comes back as a mid tone of that hue. The About dialog's body, its secondary button, and that button's hover all derived to within a couple of points of #93a4be, and the dialog rendered as one flat periwinkle slab. Those three are restated in `light_palette()` as the light surface band with the cast kept. A tinted surface added to the palette needs the same check; a role whose chroma really does carry brand or state meaning does not.
- **A widget that wants its own values gets its own roles.** Borrowed roles read as a coincidence that holds in one scheme; a named family states the relationship the corrections have to preserve. The color picker's tab bar borrowed dialog and field roles, got caught by the tab correction above, and now has a `picker_tab_*` family. `ui_light_scheme_color_picker_selected_tab_reads_as_selected` renders the bar in both schemes and pins it.
- **Never hand token text to `setStyleSheet`.** Qt drops the whole declaration containing an unresolved token, silently, so a rule just stops existing. Use `set_themed_style` / `append_themed_style` from `src/ui/theme_qss.hpp`; the shared builders return `ThemedQss` so the mistake is a compile error, and `ui_no_widget_ships_unresolved_theme_tokens` catches inline blobs that slip through. Rich-text labels with colored links use `set_themed_label_text`.
- **`append_themed_style` preserves order.** A widget's sheet is an accumulation (chrome, then the dialog's own block, then `dialog_spinbox_button_style()` last), and the stored template replays in the same order on a scheme change. A dialog that rebuilds its whole sheet reads the accumulated template back with `themed_style_template`, never `styleSheet()`.
- **A scheme change needs no `setIcon` call.** Icons are backed by engines in `src/ui/icon_theme.hpp` that resolve colors when painted, so existing `QIcon`s render correctly on their next repaint. Do not "fix" this by rebuilding icons.
- **Never bake a color into a `QIcon`.** Painting a glyph into a `QPixmap` and wrapping it freezes whichever scheme was active when the icon was built. One-off procedural marks go through `themed_glyph_icon` in `src/ui/action_icons.hpp`, which hands the glyph its ink at paint time. The same applies to a widget that paints its own chrome: read `theme()` inside `paintEvent`, never a literal.
- **If you cache a pixmap derived from `theme()`, drop it on a scheme change.** Qt sends no event for a palette-struct change. `MainWindow::apply_color_scheme` clears the layer, channel, and path thumbnail caches for this reason.
- **Checkable and disabled QPushButtons are styled globally.** The application sheet defines `QPushButton:checked` (the `accent_checked_*` trio plus `text_on_accent`, the same roles as the Options-bar checked rule) and `QPushButton:disabled` (the `field_*_disabled` trio). Do not add per-dialog copies of these states; a new checkable dialog button gets the armed look for free.
- **Some colors deliberately do not follow the scheme.** The transparency checkerboard depicts alpha and stays light in both themes, like every other editor, so "grid means transparent" remains a learned signal. Marching ants, the clone-source marker, and tool cursors are black-plus-white pairs engineered to read over arbitrary artwork; tying them to UI colors would make a selection vanish over matching pixels. Saved grid and guide colors are user data. The app icon and splash artwork composite against the OS, not Patchy's chrome. Adjustment-layer glyphs encode meaning through their own internal contrast (the Threshold glyph *is* a black/white split).

Authoring icons: 32x32 viewBox, lowercase hex, drawn from the ten-color vocabulary in `icon_color_roles()` (`icon_ink` plus nine accents). `ui_icon_color_map_covers_every_authored_color` rejects anything outside it. The two paint-swatch icons and the seven referenced from QSS by `url()` are exempt and listed explicitly in `icon_theme.cpp`.

## Hotkeys

Application-level QAction shortcuts must be registered through `MainWindow::register_hotkey(action, "stable.id", default_seq)`, backed by `HotkeyRegistry` in `src/ui/hotkey_registry.hpp`. Never call `setShortcut` or `setShortcuts` directly on an app-level action. Command ids are persisted and must never be renamed. Two commands must not share a default shortcut; `ui_hotkey_defaults_have_no_conflicts` enforces this.

A canvas tool that needs a key owned by an application shortcut must accept the `ShortcutOverride` in `CanvasWidget::event()` while active. Magnetic Lasso Delete/Backspace is the reference pattern; see [selection-tools.md](selection-tools.md).

Modifier keys are named per platform, on two surfaces with two mechanisms (September 2026). The shortcut CHIP beside an action name (`"Direct Select (Shift+A)"`) comes from `QKeySequence::NativeText` in `refresh_action_tooltip` and already renders the macOS glyphs; nothing to do there. A SENTENCE that names a key to hold (a status hint, a tooltip detail, an accessibility description) writes the tokens `%CTRL%` and `%ALT%` and resolves through `resolve_modifier_names` from `src/ui/modifier_names.hpp`, which spells Ctrl/Alt on Windows and Linux and Command/Option on macOS. That spelling is correct, not cosmetic: Patchy does not set `AA_MacDontSwapCtrlAndMeta`, so Qt maps `Qt::ControlModifier` onto the Command key and a hint reading "Ctrl+T" on Windows describes Command+T on a Mac. Shift, Delete, and the mouse verbs are the same everywhere and take no token. Never write a bare "Ctrl" or "Alt" into a user-facing sentence, and never spell both names by hand ("Alt/Option-drag" was the pre-helper workaround). The two tool-hint sources in `main_window_actions_tool_palette.cpp` are translated later at a single choke point each (`refresh_action_tooltip` and the tool activation handler), which is why the tokens are not `%1`-style: `QString::arg` only consumes `%` followed by a digit, so a sentence can carry both. Translations keep the tokens verbatim. Tests that assert on such a sentence build the expectation through the same helper; `ui_path_tools_show_activation_hints_and_tooltips` is the reference.

## Dialog lifecycle

Dialogs that react to closing must funnel every path through `done()`. `reject()` hides a dialog without a QCloseEvent, so closeEvent-only cleanup misses the chrome X and Esc. Do not override `reject()` to call `close()`: `QDialog::closeEvent` calls `reject()` and treats a still-visible dialog as vetoing the close.

New non-modal dialogs must use `run_non_modal_dialog`. It rejects child dialogs when their parent finishes and applies the macOS above-parent native anchor. `request_patchy_color` permits one picker at a time; transient pickers retain their own position-memory group. Every choose-a-color prompt in the app goes through it (palettes, color names, hex, live preview), never `QColorDialog`; it returns opaque RGB, so a value that carries alpha keeps it beside the picker (Preferences pairs the grid and guide color buttons with opacity spin boxes; a script form's color field keeps the alpha it was given).

A dialog function that arms async work must disarm it from a `qScopeGuard` declared after the dialog, not only after the nested event loop returns. These dialogs are stack `QDialog`s, and their completion callbacks capture the function's frame by reference, but the worker posts the completion to `QCoreApplication` rather than to the dialog, so Qt never auto-disconnects it: every other `connect` in the function uses `&dialog` as its context and is cleaned up, and that one posted call is not. A cleanup statement placed after `run_non_modal_dialog` or `exec_dialog` is skipped when an exception unwinds the frame instead, and the completion then runs against destroyed widgets from a later, unrelated event loop. This was a real crash (August 2026): a `CHECK` throwing inside a `QTimer::singleShot` lambda in the filter gallery faulted in a completely different test. The guards live in `request_visual_filter_gallery` (proxy preview and thumbnails), `request_filter_settings`, and `run_raw_develop_dialog`; every `close_*` helper they call is null-safe and idempotent so the normal-path call can stay. Do not use `QObject::destroyed` instead: `QWidget`'s destructor deletes its children before `~QObject` emits that signal, so a handler cannot touch the dialog's timers, and the dialog is destroyed last anyway. `ui_filter_gallery_unwinding_call_disarms_in_flight_renders` pins the gallery case. Leaving a running `run_non_modal_dialog` loop by exception has one supported path: catch at the slot boundary and pass `std::current_exception()` to `unwind_non_modal_dialog_loop` (`src/ui/dialog_utils.hpp`), which quits the innermost loop and lets `run_non_modal_dialog` rethrow on its own frame. Throwing straight through the dispatcher is unsupported by Qt: MSVC happens to unwind, macOS terminates inside the CFRunLoop frames.

Show a dialog through `exec_dialog` or `run_non_modal_dialog` rather than calling `QDialog::exec` directly. Both funnel into `place_dialog`, which centers the dialog on its owner (or restores its remembered position) and, on wasm, shrinks it to the browser canvas first: a browser has no window manager to rescue a dialog taller than the page, so an unclamped one leaves its button row unreachable below the fold. When even the layout minimum exceeds the canvas, the wasm path moves the dialog content into a scroll area so the shrink can proceed. A dialog with no objectName gets the centering and the clamp but no position memory, which is why converting a raw `exec()` call site is safe. See [wasm.md](wasm.md).

Dialog spin boxes that retain their minus/plus buttons must append `dialog_spinbox_button_style()` from `src/ui/dialog_utils` to the dialog stylesheet after all children exist.

A font derived from another one (a smaller detail line, a larger headline) goes through `scale_font_size`, `scaled_font`, or `offset_font` in `src/ui/dialog_utils.hpp`. Never write `font.setPointSizeF(font.pointSizeF() * k)` directly: the size lives in points or pixels and the unused accessor returns -1, so that line is a no-op plus a Qt warning wherever the inherited font is pixel-defined (macOS). See the cross-platform rules in [platform.md](platform.md).

## Item-widget rows and selection

Rows installed with `QListWidget::setItemWidget` must paint their own selection because an opaque row widget hides `::item:selected` QSS. Give the list `padding: 0` on `::item`, make the row's child containers transparent, and keep the global `QCheckBox { border: none; }` rule for correct macOS layout.

Every plain QWidget container within the row must have a transparent background in application QSS. With a global QWidget background rule, QStyleSheetStyle applies `WA_StyledBackground`, so `setAutoFillBackground(false)` alone does not prevent the container painting over its row.

Everything specific to the Layers panel (row styling, thumbnails, rebuild order and absent rows, click selection including the Move tool's canvas-side Ctrl+click toggle, the folder disclosure arrow, double-click editors, and the visibility eye) lives in [layer-panel.md](layer-panel.md); read it before changing layer rows.

`bind_widget_text` on a QLineEdit binds the placeholder text, not `text()`, since line edits carry user data in `text()`.

## Options bar

All options-bar controls share one fixed 26 px row height. QStyleSheetStyle inflates QToolButton size hints, so options-bar buttons need explicit min/max height caps. Free-transform and warp sessions own the row and hide per-tool widgets.

Options-bar numeric controls must use `configure_toolbar_spinbox`. Set range, decimals, prefix, and suffix before calling it because its width is only a minimum and the helper expands for the widest value plus its trailing chevron. Do not duplicate the popup or substitute a plain spin box. Dialog and transform-session fields use `configure_dialog_spinbox` when the range popup is inappropriate.

When the typed range is wider than the useful drag range, set `kToolbarSpinboxSliderMaxProperty` from `dialog_utils.hpp`; do not narrow the spin-box range. Text Size, for example, accepts 10000 pt while its slider normally stops at 200 and expands if the current value is higher.

Application-wide tool state follows the `current_*` mirror pattern in MainWindow: update the member from the control signal, apply it both during new-session setup and `activate_document_tab`, and update the mirror in `load_tool_settings()` under `QSignalBlocker`. Updating only the current canvas desynchronizes newly created or reactivated documents.

## Status bar

QStatusBar hides non-permanent widgets whenever a message is displayed. Patchy's left-side status widgets are manually positioned children of `ZoomStatusBar`; never add them with `addWidget()`.

Blocking refusals and failed operations use `MainWindow::show_status_error` or `CanvasWidget::report_status_error`, which add the red flash and warning icon. Informational confirmations, cancellations, no-op results, and readouts use `showMessage`. Status text reaches `currentMessage()` unchanged, so tests may assert exact strings.

## Standing UI rules

- The right dock stack's minimum width is measured, not hardcoded: `MainWindow::update_right_dock_minimum_width` derives it from the layers panel layout minimum (whose widest row is the localized blend/opacity row) plus the dock chrome measured from a laid-out dock. At that minimum the blend/opacity row fits exactly, so the Fill spin box's right edge lines up with the name-filter edit. Anything that widens a layers-panel row (new controls, longer prefixes) moves the minimum automatically; do not add a competing hardcoded width, and keep staged `setSidePanelWidth` values in tests comfortably above the measured minimum.
- Right-dock panel heights must never pin the window taller. A collapsed dock pins min == max to its title bar's styled sizeHint (every collapsed panel is the same strip, no dead band under the header; `refresh_collapsed_right_dock_heights` re-pins after the stylesheet lands, since QSS title borders grow the sizeHint). Expanding demands the panel's preferred height through a temporary minimum, the only request the dock area is guaranteed to honor; one event-loop hop later `MainWindow::handle_right_dock_panel_toggled` releases the demand back to the low floor (the dock keeps the granted height), activates the layout, and re-clamps the window to the screen only when the toggle actually grew it. Panels whose content can outgrow their share scroll internally (Properties' scroll area, the Palette panel's `paletteScrollArea`); never give a right-dock panel an unbounded hard minimum height. `ui_right_dock_panels_expand_within_window_height` and `ui_collapsed_right_docks_have_uniform_title_height` pin this. `create_docks` also sets `QMainWindow::GroupedDragging`: it is what lets a dock dragged into a tab group be dragged back out by its tab (`ui_tabbed_right_dock_drags_out_by_tab`). Its other half, floating a whole tab group by its title, is deliberately suppressed: `handle_right_dock_title_drag_event` consumes title presses on tabbed right docks and detaches that single dock under the cursor instead (`ui_tabbed_dock_title_drag_floats_single_dock`), because Qt's floating group window is broken with custom title bars (redundant second header, undraggable blank strip, a grab-point jump). Untabbed docks keep Qt's native title drag. Floating groups can still be formed deliberately by dropping one floating dock onto another; `handle_dock_group_window_event` then gives that window, and equally a single floating right dock (styled via its `floatingChrome` property), the chrome Qt omits: contents margins widen Qt's few-pixel frame into a real strip painted as divider chrome with a border (readable over a same-value canvas), presses on that strip resize the window with matching edge cursors, and blank areas (the tab bar's empty stretch included) drag it (`ui_dock_group_window_drags_by_blank_chrome`, `ui_floating_right_dock_auto_expands`). Every dock in the column hosts the 7px `rightDockResizeHandle` overlaying its left edge (`install_right_dock_width_handle`), so the column divider is grabbable along its entire height; title bars and panel contents carry a matching left inset to stay clear of it (`ui_right_dock_contents_clear_width_handle`). Floating docks are exempt: the width pin skips them, the handle hides until they re-dock, and a toggle nudges the floating window's size (the `resync_native_frame_geometry` remedy) because its backing store keeps stale pixels over the newly exposed panel. Floating panels are also always expanded: a collapsed strip's min == max pin cannot be plugged into a floating tab group (the docked partner blinks away), so pulling a panel out auto-expands it and the collapse toggle only shows while the panel sits in the main window's column (`ui_floating_right_dock_auto_expands`).
- The layer context menu always keeps **Edit Layer Styles...** as its first item; `ui_layer_context_menu_keeps_edit_styles_on_top` enforces this.
- The menu-bar Layer menu groups its new-layer, layer-mask, and move/flip sets into the `layerNewMenu`, `layerMaskMenu`, and `layerArrangeMenu` submenus so the whole menu fits a short browser viewport in the wasm build; `ui_main_window_renders_color_controls` bounds its direct row count at 23, so add new Layer commands inside an existing submenu or group rather than as top-level rows. The Layer Mask submenu mirrors the layer context menu's item order and separators; keep the two in step.
- The Open Recent File menu leads with a filter row (`fileOpenRecentFilterEdit`, hosted by the codebase's only QWidgetAction). `rebuild_recent_files_menu` recreates the row on every rebuild, so filter state always resets; `aboutToShow` focuses the edit so typing filters immediately, and the MainWindow application event filter forwards Up/Down and defers Enter through a `QTimer::singleShot(0)`. The deferral is load-bearing: triggering an entry synchronously rebuilds the menu and deletes the edit while its own key event is mid-delivery. The row is absent under a native menu bar; see [platform.md](platform.md). Tests must find file entries by non-empty `data()`, never by position. The start panel's `startPanelRecentList` lists the same 200 entries in a box fixed at 8 rows and scrolls for the rest, and its right-click menu comes from the same `MainWindow::show_recent_path_context_menu` the Open Recent menus use, so the two can never drift apart. Its own filter edit (`startPanelRecentFilterEdit`, beside the Recent Files label) applies the menu's matching rule, all whitespace-separated tokens anywhere in the native path, and Enter opens the top row; `StartPanel::showEvent` clears it and takes focus, so the panel always reappears unfiltered and ready to type. A filter that matches nothing leaves one placeholder row in the box whose `kRecentPathRole` path is empty, which is what the delegate, the click handler, and the context-menu handler test for; never give a real row an empty path.
- A QWidget subclass that paints its own content inside a themed container needs `background: transparent` in that container's sheet. The global `QWidget` rule sets `WA_StyledBackground` on it, so the whole slot fills with `window_bg` and a window-colored rectangle appears wherever the widget's own painting leaves a margin. The About dialog's artwork (`splashArtwork`) is the reference case; `ui_light_scheme_about_dialog_reads_as_a_light_surface` pins it.
- Read modifier state folded from the current event, not `QApplication::keyboardModifiers()`. Live application state can lag event filtering, and offscreen tests retain synthetic modifier bits.
- Application-wide QSS and hotkey changes require both full release test suites under the handoff rules in `AGENTS.md`.
- Interface scale is one global `QT_SCALE_FACTOR` on desktop, set by `apply_gui_scale_factor` in `src/app/main.cpp` before `QApplication` exists. On wasm it is applied browser-side by the shell page instead (a CSS-transformed iframe; see [wasm.md](wasm.md)), because the wasm plugin's pointer path ignores the Qt scale factor and any non-1 factor offsets every click. The steps (67% through 200%), the platform default, and the read/write helpers live in `src/ui/app_settings.hpp`; the shell page duplicates the step list and the 75% web default, so changes to either must touch both. Steps below 100% shrink the whole UI. A change takes effect on the next start, which in the browser is a page reload, and the settings key `preferences/guiScalePercent` is a compatibility contract.
- Never fix a QToolBar's size along its perpendicular axis (the width of a vertical bar). The overflow extension button reveals hidden items by temporarily enlarging that axis into extra rows or columns, and a fixed size clamps the expanded geometry into a clipped sliver. Use a minimum size instead; the tool palette's 43 px minimum width is the reference (`ui_tool_palette_extension_button_expands_palette` pins the expansion). A dynamic minimum is fine: `ToolPaletteExpansionReservation` raises the palette's minimum to the measured expanded width while the extension button is checked, which makes the layout reserve the extra columns instead of letting them overlay the canvas, and restores 43 on collapse.
- Chrome buttons the style positions itself are exempt from the global QToolButton minimums. QTabBar's overflow scroll arrows get their geometry from the fixed scroll-button metric, not from a layout, so the global 26 px minimums inflated the drawn box and clipped the right arrow at the bar's edge; the `QTabBar QToolButton` rule zeroes the box back out and `QTabBar::scroller` pins the two-button area to 32 px on every platform. The base styles draw the glyph differently: Windows/Linux paint a fixed-size arrow (padding must stay 0 or it collapses toward a dot), macOS scales it edge-to-edge (a mac-only 2 px padding gives it margins). Relatedly, `suppress_native_tab_bar_base` runs on every platform: the native base is covered by the stylesheet everywhere except the scroller area, where it surfaced as a 1px white line through the transparent buttons once tabs overflowed. `ui_document_tab_scroll_arrows_render_inside_the_bar` pins all of this.
- Canvas scroll bars are direct `CanvasWidget` children with load-bearing objectNames `canvasHorizontalScrollBar`/`canvasVerticalScrollBar` (theme QSS and UI tests find them by name). Their range mirrors the 10%-minimum-visible pan clamp (`pan_axis_range` in `canvas_widget_view.cpp`), so bar scrolling and hand-tool panning can never disagree, and the bars stay live even when the document fits the view.
- Every scroll bar in the application is styled by `photoshop_style()`, on every platform. None of them can be left native: the global `QWidget` rule at the top of that sheet sets a background on every widget, scroll bars included, and once QSS touches a scroll bar `QStyleSheetStyle` owns the whole complex control, so one without subcontrol rules fills its groove with the window background and lets the base style draw the rest on top. That looked passable against Dark's near-black surface and erased the bar completely in Light. The canvas bars differ from panel bars in exactly two ways: their track slaves to `canvas_backdrop` rather than the window surface, and they carry the `scroll-dither` texture, which is authored for the pasteboard gutter and is too dark to lay over a light panel. The dither is opaque, so it replaces the track color rather than tinting it. `icons/light/scroll-dither.svg` is authored around the same value as `canvas_backdrop`/`canvas_scrollbar_track` so the gutter, the corner square between the bars, and the pasteboard all read as one surface, and moving that role means re-authoring the asset. One handle fill (`scrollbar_handle_bg`) serves both families, so it has to clear the darker of the two tracks, which in Light is the pasteboard gutter. `ui_light_scheme_panel_scroll_bar_handle_reads_against_track` renders a real bar and pins the result. The `margin: 0` on the `QScrollBar` widget rules is load-bearing: QStyleSheetStyle only owns the groove rect when the widget rule has box properties, and otherwise asks the native style, which reserves room for the arrow buttons the sheet removes; on a short bar (a squeezed panel) that phantom reservation leaves the groove smaller than the styled handle, the drag span goes negative, and every handle drag snaps the value to the minimum. A `::groove` rule does not help (Qt never consults it for geometry). `ui_short_panel_scroll_bar_drags_by_handle` pins this.

Application-wide fill, quick-selection, interpolation, and vector options transfer
when the active canvas changes. Language changes in Preferences apply only on OK.
The color panel accepts named colors and CSS `#RRGGBBAA` (plus `#RGBA`); its stored
foreground/background colors remain opaque RGB. History navigation is refused
during a live pointer gesture, and focus loss clears the drag latches.

Recent files and folders include successful interactive, scripted, headless and
MCP document opens and saves, including saved flat copies. Saving records the
containing folder too. `recent_history_settings()` shares the persistent history
with owned MCP workspaces while their other preferences stay temporary. Native
updates lock the complete read/modify/write transaction and merge against the
latest stored list, so another process's additions or Clear command survive.
The File menu and Save As refresh before opening; the empty start panel refreshes
every two seconds while no popup is active and when it reappears. Rebuilds occur
only when the stored list changes, preserving a live start-panel filter.
