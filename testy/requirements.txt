# Testy's Python dependencies (any Python 3.11+).
pywin32          # Photoshop COM driver + the modal-dialog guard (win_dialogs.py)
Pillow           # render comparison
numpy            # render comparison
selenium         # Photopea driver (headless Chrome; Selenium Manager fetches chromedriver)
pywinauto        # Affinity driver (background UI Automation)
